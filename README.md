# YTNotifier v1.7.1 - YouTube新着動画通知システム

YouTube Data API v3 を利用し、指定チャンネルの新着動画を定期監視・通知する WPF 常駐アプリ。
<img width="475" height="300" alt="01" src="https://github.com/user-attachments/assets/19297cf1-19e9-421f-9676-39f30f586cf1" />
<img width="325" height="300" alt="02" src="https://github.com/user-attachments/assets/e8bfe037-9105-4c02-86a7-98250f38a83a" />
<img width="330" height="300" alt="03" src="https://github.com/user-attachments/assets/6a71c243-0302-47f7-90d6-59ec14e556f1" />
---

## 動作環境

- Windows 10 / 11
- .NET 8.0 以上
- YouTube Data API v3 APIキー

---

## セットアップ

### 1. ビルド

```bash
cd YTNotifier
dotnet restore
dotnet build
```

### 2. 発行（Self-Contained）

```bash
dotnet publish -r win-x64 -p:PublishSingleFile=true --self-contained
```

### 3. アイコンの準備

`Resources\` フォルダに以下の2ファイルを用意してください。

| ファイル | 用途 | 推奨サイズ |
|---|---|---|
| `app.ico` | ウィンドウ・トレイ通常時 | 16/32/48/256px 内包 |
| `app_warn.ico` | トレイ停止時 | 16/32/48/256px 内包 |
| `notification.wav` | カスタム通知音（任意） | WAV形式・任意のサイズ |


### カスタム通知音（任意）

`Resources\notification.wav` を配置すると、通知音オン時にそのファイルが再生されます。
ファイルがない場合は Windows のシステム通知音が使用されます。

---

## YouTube API キーの取得

1. [Google Cloud Console](https://console.developers.google.com) を開く
2. プロジェクトを作成または選択
3. 「APIとサービス」→「ライブラリ」で **YouTube Data API v3** を有効化
4. 「認証情報」→「認証情報を作成」→「APIキー」を選択
5. アプリの「基本設定」画面にAPIキーを貼り付けて保存

---

## ファイル構成

```
YTNotifier/
├── YTNotifier.csproj
├── App.xaml / App.xaml.cs       # 起動・テーマ切替・システムトレイ
│
├── Models/
│   └── AppModels.cs             # AppSettings / ChannelInfo / LogEntry
│
├── Services/
│   ├── SettingsService.cs       # config.json / channels.json の読み書き
│   ├── LoggerService.cs         # UIログ・エラーログ + 日別ファイル出力
│   ├── YouTubeApiService.cs     # YouTube API v3 通信・動画種別判定
│   └── MonitorService.cs        # 定期監視・トースト通知
│
├── Views/
│   ├── MainWindow.xaml          # サイドバー + 3ページ構成のUI
│   └── MainWindow.xaml.cs       # UIロジック・チャンネル行コード生成
│
├── Themes/
│   ├── LightTheme.xaml          # ライトテーマ
│   ├── DarkTheme.xaml           # ダークテーマ
│   └── CommonStyles.xaml        # 共通スタイル（フラットUI）
│
└── Resources/
    ├── app.ico                  # トレイ・ウィンドウアイコン（通常）
    └── app_warn.ico             # トレイアイコン（停止中）
```

---

## データ保存先

`%APPDATA%\YTNotifier\`

| ファイル | 内容 |
|---|---|
| `config.json` | APIキー・テーマ・通知設定・ウィンドウサイズ・サイドバー状態等 |
| `channels.json` | 確認チャンネルリスト（順序・種別設定・表示名含む） |
| `logs\YYYY-MM-DD.log` | 日別ログファイル（全件累積） |

---

## 主要機能

### チャンネル管理
- チャンネルID / ハンドル名（`@name`） / URL で追加
- 追加前にプレビュー確認（チャンネル名・登録者数・アイコン）
- **ドラッグアンドドロップ**でリスト内の順序を変更
- チャンネルアイコンをクリックで有効な種別の最新動画をブラウザで開く
- 右クリックメニュー:
  - 🔔 NEWバッジを消す
  - ✏ 名称を変更（表示名をカスタマイズ）

### 通知種別フィルター
各チャンネルごとに **動画 / Short / ライブ** の通知種別を個別に設定可能。

| 設定例 | クリック時の動作 |
|---|---|
| 動画・ライブ ON | 最新がライブ→ライブを開く、最新が動画→動画を開く |
| 動画のみ ON | Short・ライブをスキップして最新の通常動画を開く |
| 全て OFF | チャンネルページを開く |

### 監視・通知
- 1〜60分間隔（設定変更可）でチャンネルを**並列チェック**
- 複数の新着があっても通知は**最新1件のみ**（多重通知防止）
- 動画種別を `videos.list` + `liveStreamingDetails` で正確に判定
  - Short: 2分30秒（150秒）以内、またはタイトル・説明・タグに `#Shorts` / `#Short` を含む
  - ライブ/アーカイブ: `liveStreamingDetails` フィールドが存在する動画
- **NEW バッジ**: 新着検出時にチャンネル名横に表示、クリック or 右クリックメニューで消去
- Windows トースト通知をクリックで動画をブラウザ再生（種別フィルターに従い最新動画を開く）
- **通知音**: `Resources\notification.wav`（任意）または Windows の `notify.wav` を25%音量で再生

### UI
- **フラットデザイン**（シャドウなし・グラデーションなし）
- ライト / ダークモードをリアルタイム切替
- **カスタムタイトルバー**（最小化 / 最大化 / 閉じる）
- 折り畳み可能なサイドバーナビゲーション（状態を保存・復元）
  - チャンネルリスト・動作ログ・基本設定
- ウィンドウサイズ・位置を終了時に自動保存
- チャンネル追加エリアを折り畳み可能
- 常に前面に表示オプション

### 動作ログ
- 各チャンネルの**最新ステータス1件のみ**をUIに表示（起動毎にリセット）
- **エラーログセクション**（動作ログページ下部）
  - Warning・Error レベルのみ表示
  - ローテーションなし（全件保持）
  - クリアボタンで手動消去
- ファイルには全件累積出力（`logs\YYYY-MM-DD.log`）

### ログメンテナンス（基本設定内）
- ログファイル数・合計サイズ・最古〜最新の日付を表示
- **自動削除**: 起動時に保持期間より古いログを自動削除
- **保持期間**: 1日 / 3日 / 5日 / 7日 / 14日 / 30日（推奨）/ 60日 / 90日 / 無制限
- **手動削除**: ボタンクリックで即時削除・削除件数とサイズを表示
- **フォルダを開く**: ログフォルダをエクスプローラーで開く

### システムトレイ
- `System.Windows.Forms.NotifyIcon` による確実なトレイ常駐
- 右クリックメニュー: ウィンドウを開く / 今すぐチェック / 監視開始・停止 / 終了
- 「タスクトレイに格納」OFF 時は × ボタンでプロセス完全終了

---

## APIクォータ管理

YouTube Data API v3 の無料枠は **10,000ユニット/日**。

| 操作 | コスト |
|---|---|
| チャンネル情報取得（プレビュー） | 1ユニット |
| 新着チェック（activities.list） | 1ユニット × チャンネル数 |
| 動画種別判定（videos.list） | 1ユニット × チェック時の新着数 |

**推定消費量の計算式**:
`(1440 ÷ 間隔分) × チャンネル数 × 2ユニット ≤ 10,000`

| 構成例 | 推定消費量 |
|---|---|
| 10ch・5分間隔 | ≈ 5,760ユニット/日 |
| 10ch・10分間隔 | ≈ 2,880ユニット/日 |
| 20ch・10分間隔 | ≈ 5,760ユニット/日 |
| 50ch・30分間隔 | ≈ 4,800ユニット/日 |

アプリが自動的にクォータ超過を検知し、監視間隔を安全な値に調整します。

---

## 注意事項

- Windows 10 以降の **Windowsトースト通知**を使用します
- Self-Contained 発行時は `--self-contained` オプションを付けてください
- APIキー未設定の場合、チャンネル追加・監視機能は動作しません
- MinHeight / MinWidth はそれぞれ 460px に設定されています

---

## 変更履歴

### v1.7.1
- 動作ログからチャンネル名のない行の `[]` 表記を除去
- 動作ログから時刻表記を除去
- 種別トグル（動画・Short・ライブ）を編集モード中のみ変更可能に変更
- Ctrl押下中のMinWidthを330pxに制限（従来は完全解除）
- Ctrl非押下中のリサイズで最小サイズを下回った場合即座に強制復元
- ABOUTページに「Created by Coco」を追加
- チャンネルリストの×ボタンを編集モード中のみ表示
- 基本設定のサブナビを横並び（アプリ・監視・ログ・API・ABOUT）に変更
- 基本設定のチェック間隔を 3/5/10/15/20/30/60分 に変更、クォータ超過項目を選択不可に
- ログ保持期間を 0日（当日のみ）・1日・3日・5日・7日・無制限 に変更
- 終了時に LastCheckedVideoId を保存、起動時チェックで新着を検出・通知
- 「確認リスト」を「チャンネルリスト」に名称変更
- サイドバー展開幅を140pxに変更

### v1.7.0
- 「確認リスト」を「チャンネルリスト」に名称変更
- サイドバー展開幅を 120px → 140px に変更
- 動作ログページからエラーログセクションを削除
- 基本設定を縦サブナビゲーション → 横並びサブナビゲーション（アプリ・監視・ログ・API・ABOUT）に変更
- 基本設定ページにタイトル行を追加
- チェック間隔を 3分・5分・10分・15分・20分・30分・60分 に変更
  - 起動時にチャンネル数でクォータ計算を行い、超過する間隔を選択不可に設定
- ログ保持期間を 0日（当日のみ）・1日・3日・5日・7日・無制限 に変更
  - 0日設定時は当日分のログのみ保持
- 終了時に各チャンネルの LastCheckedVideoId を保存
- 起動時チェックで前回終了後の新着動画を検出・通知

### v1.6.0
- 基本設定に通知音オプションを追加（デフォルト ON）
  - `Resources\notification.wav` がある場合はそのファイルを25%音量で再生
  - ない場合は Windows の `notify.wav` を25%音量で再生（NAudio使用）
  - デスクトップ通知OFF + 通知音ON → 音のみ再生
- トースト通知クリック時の動作をアイコンクリックと同一に変更
  - 種別フィルターに従い最新動画を開く・NEWバッジを自動消去
- サイドバーの「監視停止」ボタンを削除し、監視中/停止中バッジをトグルボタン化
  - クリックで監視開始/停止・動作ログページへ自動遷移
- サイドバー左下にピンボタンを追加（常に前面表示トグル）
  - 通常時はテーマカラー・ピン留め時は赤色表示
- 基本設定から「常に前面に表示」項目を削除（ピンボタンで代替）
- APIキー入力欄をマスク表示に変更
  - 保存済み時は●マスク表示（コピー不可）・変更ボタンで編集可能に
- チャンネル追加ウィンドウのサイズを 520×190px 固定に変更
- チャンネル行左端にNEWバッジを移動（アイコン左に横書き表示）
- 基本設定にコンパクトモードを追加（ダークモードの下）
  - ON時: アイコン（通常の1/2サイズ）＋チャンネル名のみ表示、行高36px
  - OFF時: 通常表示（行高60px）
- 編集ボタン押下後の完了ボタン押下で元のスタイルに正しく復帰するよう修正
- チャンネル行左端のNEWバッジを削除（アイコンを左詰めに変更）
- 基本設定にコンパクトモードを追加（ダークモードの下）
  - ON時: アイコン（通常の1/2サイズ）＋チャンネル名のみ表示・行高36px
  - OFF時: 通常表示（行高60px）
- 基本設定の各オプションのデフォルト値をOFFに変更（デスクトップ通知・タスクトレイ格納・通知音）
- APIキー未設定時の初回起動でセットアップウィンドウを表示（メインウィンドウはAPIキー保存後に表示）
- チャンネルリストの「今すぐチェック」「編集」ボタンをアイコンのみに変更（ToolTipでラベル表示）
- 初回起動時（config.json未存在）のウィンドウ横幅を405pxに設定
- チャンネルアイコンのディスクキャッシュを実装
  - 保存先: `%APPDATA%\YTNotifier\icons\` （URLのSHA1ハッシュをファイル名に使用）
  - 起動時はディスクキャッシュから読み込み（ネットワーク通信なし）
  - 未キャッシュ時はバックグラウンドでダウンロードして保存・自動反映
- コンパクトモード時に行全体クリックで最新動画を開く（通常モードのアイコンクリックと同一動作）
- コンパクトモード時も左端のAccentBrushバーで新着通知を表示
- 通知音の動作
  - `Resources\notification.wav` がある場合はそのファイルを25%音量で再生
  - ない場合は Windows の `notify.wav` を25%音量で再生
  - 音量制御に NAudio ライブラリを使用
- トースト通知と通知音を独立制御
  - トースト ON + 通知音 ON: トースト表示 + notify.wav 再生
  - トースト ON + 通知音 OFF: トースト表示のみ（無音）
  - トースト OFF + 通知音 ON: notify.wav 再生のみ
  - トースト OFF + 通知音 OFF: 何もしない
- トースト通知クリック時の動作をアイコンクリックと同一に変更
  - 種別フィルターに従い最新動画を開く
  - NEWバッジを自動消去

### v1.5.1
- チャンネル追加ウィンドウのサイズを固定（520×190px）
- チャンネル追加ウィンドウのレイアウトをフラットUIに統一
- チャンネル追加ウィンドウのプレビュー欄を左側に配置
- 基本設定の各項目から説明文・セクションヘッダーを削除
- 「今すぐチェック」ボタンをチャンネルリストページ内に移動（サイドバーから削除）
- サイドバーの開閉状態に応じてウィンドウ最小幅を動的に変更
  - サイドバー展開時: 530px / 折り畳み時: 405px

### v1.5.0
- APIクォータ自動管理機能を追加
  - チャンネル数×監視間隔から1日の消費ユニット数を自動計算
  - クォータ超過時に監視間隔を推奨値へ自動調整・警告表示
  - チャンネル追加・削除時にも自動調整
  - 基本設定に推定消費量をリアルタイム表示（緑/黄/赤でカラー変化）
- 基本設定にログメンテナンス機能を追加
  - ログファイル統計表示（ファイル数・サイズ・期間）
  - 自動削除トグル（起動時に古いログを削除）
  - 保持期間設定（1/3/5/7/14/30/60/90日・無制限）
  - 手動削除ボタン・フォルダを開くボタン
- 動作ログページから「フォルダを開く」ボタンを削除（ログメンテナンスに移動）
- チャンネルリストに編集モードを追加
  - 編集ボタン押下中のみドラッグアンドドロップで並び替え可能
  - 編集モード中はアイコンクリック・種別トグルを無効化
  - ドラッグ中にドロップ先を青い横線インジケーターで表示
  - トースト通知の重複クリック問題を修正（ハンドラを1回のみ登録）

### v1.2.2
- コードリファクタリング（MainWindow.xaml.cs を1240行→1007行に削減）
- チャンネル行の責務を分割（BuildIconBorder / BuildInfoPanel / BuildActionsPanel）
- 定数化・重複コード除去・ヘルパーメソッド整理
- サイドバーメニューの順序変更（チャンネルリスト → 今すぐチェック → 監視停止 → 動作ログ → 基本設定）
- サイドバー折り畳み時のアイコン位置を固定（44px幅の中央に常時配置）
- トースト通知クリック時にNEWバッジを自動消去するよう改善
- 監視中にサイドバーを折り畳んだ際、折り畳みボタンのアイコンを監視中カラー（緑）で表示
- 基本設定にログメンテナンス機能を追加（自動削除・保持期間設定・統計表示・手動削除）
- 動作ログページから「フォルダを開く」ボタンを削除（ログメンテナンスに移動）

### v1.2.1
- Short判定を2分30秒（150秒）以内に変更
- #Shortsタグが含まれる動画もShort判定に追加

### v1.2.0
- ドラッグアンドドロップによるチャンネル並び替えを追加
- チャンネルアイコンクリックで最新動画を開く（行全体クリックから変更）
- エラーログセクションを動作ログページ下部に追加
- チャンネル右クリックで名称変更・NEWバッジ消去が可能に
- サイドバー折り畳み状態を保存・復元するよう改善
- 常に前面に表示オプションを追加
- ダークモード対応を全UIに拡充

### v1.0.0
- 初回リリース
